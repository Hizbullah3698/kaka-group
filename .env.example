# Neon Postgres connection string (used by lib/db.ts and scripts/migrate.ts)
DATABASE_URL=

# Resend API key (used by lib/email.ts) — see setup steps in the project notes
RESEND_API_KEY=

# Where enquiry notification emails are sent
CONTACT_NOTIFICATION_EMAIL=
